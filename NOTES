#To plot aligned stuff
#Get extended/reference scale
alignObj<-readAlign(filename)
refScale<-alignObj[[1]]
refSeq<-alignObj[[2]]
#Convert the db with respect of the aligned reference
ndb<-convertDB(db=pep_hxb2)

#The user trsform datas prior to the track construction and use extended coordinates in plotTracks
ProbeTrack uses the sequences from the 'aligned' slot of the db
All other tracks can be used just as usual



#in convertDB, refScale probably cannot be used instead of the align file because the function still needs a refseq.
#pep_hxb2 in convertDB => NOTE when R CMD check Pviz
